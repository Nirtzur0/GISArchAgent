# CI Manifest

## Current State

- No CI workflow is committed under `.github/workflows/` as of 2026-02-06.

## Suggested Baseline (Optional Follow-Up)

If/when CI is added, the baseline command mapping should be:

- Default check: `python -m pytest`
- Optional split jobs:
  - Unit: `python -m pytest -m unit`
  - Integration: `python -m pytest -m integration`
  - E2E: `python -m pytest -m e2e`

If CI is introduced, add a guardrail check:
- Any PR changing tests/CI/runtime behavior must also update:
  - `/Users/nirtzur/Documents/projects/GISArchAgent/docs/implementation/00_status.md`
  - `/Users/nirtzur/Documents/projects/GISArchAgent/docs/implementation/checklists/04_test_stabilization.md`

